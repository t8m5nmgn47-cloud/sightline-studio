# dependencies
node_modules/

# build / platform
.vercel/
supabase/.temp/

# OS & editor cruft
.DS_Store
*.log
.fuse_hidden*
*.swp
Thumbs.db

# Research/scraped artifacts — analysis only, not for production
assets/harvest/
assets/captured/*/pages/
engine/preview/national/

# Generated per-prospect demos (build output; publish a specific one with: git add -f demos/<slug>)
demos/

# stray local WIP (not for deploy)
index-redesign.html
engine/merge-national.mjs

# local secrets (API keys) — never commit
.sightline.env
.vercel
.env*
