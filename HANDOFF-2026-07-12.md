# Handoff — July 12, 2026 (Claude Cowork session, Kris's Mac)

*For the next session (any agent or human). Repo state at time of writing: `main` @ `0fa88404`, clean tree, local folder in sync with origin. Per CLAUDE.md law #2: `git pull` before touching anything — multiple AI sessions work this repo.*

---

## What shipped this session

### 1. Local folder recovered
The Mac checkout was 51 commits behind origin (stuck at July 6). Fast-forwarded to `c971893a`, removed a stale `.git/index.lock`, discarded a trivial timestamp-only change in `engine/preview/qa-acaciadentalgroup-com.json`.

### 2. Tax Junkie launched and integrated (the big one)
- **Repo:** `t8m5nmgn47-cloud/tax-junkie` (private) — Next.js 14 app, cloned to `~/Sightline-combined/tax-junkie`.
- **Vercel:** project `tax-junkie` (`prj_CcRrkFjFFA1wxvFLwVJijNVkH0t5`), GitHub-connected, auto-deploys on push. Deployed in **demo mode** (no Supabase env vars — visitor data stays in their browser; this is intentional per the repo's own `sightline-integration/README.md`).
- **Integration model — one domain:** tax-junkie has `basePath: '/tax'` (its commit `8d4e8c5`); this repo's `vercel.json` rewrites `/tax/:path*` → `https://tax-junkie.vercel.app/tax/:path*`. Visitors never leave sightline-studio.vercel.app. Direct hits to tax-junkie.vercel.app root redirect into `/tax`.
- **Live URLs (verified 200):**
  - https://sightline-studio.vercel.app/tax (marketing)
  - https://sightline-studio.vercel.app/tax/app/dashboard (demo app)

### 3. Homepage updates (this repo, commits `5c7337f2` → `0fa88404`)
- Tax Junkie promo section (between numbers band and Pricing) + nav links, from the kit in `tax-junkie/sightline-integration/` — links point at local `/tax` paths.
- Hero eyebrow: "Websites · Business Intelligence · Marketing · Business Accounting & Taxes".
- Hero sub-copy now mentions Tax Junkie ("keeps your books and quarterly taxes ready all year"); closing line says "whole business".
- Title / OG tags / schema.org description updated to match.
- **"Monitoring" renamed to "Business Intelligence"** — homepage only (9 spots).

## Open items, in priority order

1. **Terminology sweep (direct follow-up):** the other 15 public pages' navs and the `/how-it-works` page still say "Monitoring". The nav label "Business Intelligence" still links to `/how-it-works`. The "Security & Monitoring" +$29.99 add-on tile was deliberately left — renaming it must match `/pricing` and checkout.
2. **Vercel env vars still unset (highest business impact):** `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `ADMIN_PASS` (rotate — old one leaked in git history), `CRON_SECRET`, optional `SLACK_WEBHOOK_URL` + signal keys. Until set: lead forms 503, /admin dead, BI/intelligence crons dead.
3. **Security findings still open** (full detail in the review report from this session, `sightline-studio-review.md` in the session outputs; summary):
   - `/data/prospects.json` + `/outreach/` publicly downloadable → add to middleware matcher.
   - Supabase RLS: `authenticated using (true)` on `customers` + open magic-link signup = anyone can read the CRM. Verify signups disabled or add allowlist.
   - Live Nimble key in `.sightline.env` — rotate.
   - No rate limiting on public POST APIs; `customers` table has no tenant column.
   - Migration drift: 16 tables used by `api/`, only 4 in `supabase/migrations/`. Dump prod schema into migrations.
4. **Domain:** decision paused — staying on sightline-studio.vercel.app for now. Checked & available if wanted: `sightline-studio.com` $11.25/yr, `sightline.report` $11.99/yr, `taxjunkie.app` $9.99/yr (note: `sightline.report` is NOT owned — old canonical plans assumed it was).
5. **Tax Junkie before real accounts** (from its `sightline-integration/REVIEW-NOTES.md`): needs Supabase env + migrations, password reset, MFA enrollment, unify the two tax estimators, year-key the 2025 tax tables.

## Conventions honored (keep honoring them)
- CLAUDE.md five laws — esp. #1 look-before-done (live URLs were curl-verified this session; a human eyeball pass is still owed on the homepage) and #2 pull-before-work.
- `sightline-fixes/` (sibling folder) is a stale July 4 patch bundle, believed fully merged — candidate for archiving.
